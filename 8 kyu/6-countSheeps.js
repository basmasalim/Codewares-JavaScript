// ! Sol-1
function countSheeps1(sheep) {
  let index = 0;

  for (let i = 0; i < sheep.length; i++) {
    if (sheep[i] == true) {
      index++;
    }
  }
  return index;
}

// ! Sol-2
function countSheeps2(sheep) {
  let index = 0;

  for (let i = 0; i < sheep.length; i++) {
    if (sheep[i] == true) {
      index++;
    }
  }
  return index;
}
